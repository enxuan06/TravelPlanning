@page "/accommmodations/delete"
@using Microsoft.EntityFrameworkCore
@using TravelPlanning.Domain
@inject IDbContextFactory<TravelPlanning.Data.TravelPlanningContext> DbFactory
@inject NavigationManager NavigationManager

<PageTitle>Delete</PageTitle>

<div>
    <h2>Are you sure you want to delete this?</h2>

    <hr />
    @if (accommmodation is null)
    {
        <p><em>Loading...</em></p>
    }
    else {
        <dl class="row">
            <dt class="col-sm-2">Name</dt>
            <dd class="col-sm-10">@accommmodation.Name</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">Address</dt>
            <dd class="col-sm-10">@accommmodation.Address</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">StartDate</dt>
            <dd class="col-sm-10">@accommmodation.StartDate</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">EndDate</dt>
            <dd class="col-sm-10">@accommmodation.EndDate</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">Type</dt>
            <dd class="col-sm-10">@accommmodation.Type</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">PricePerNight</dt>
            <dd class="col-sm-10">@accommmodation.PricePerNight</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">DateCreated</dt>
            <dd class="col-sm-10">@accommmodation.DateCreated</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">DataUpdated</dt>
            <dd class="col-sm-10">@accommmodation.DataUpdated</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">CreatedBy</dt>
            <dd class="col-sm-10">@accommmodation.CreatedBy</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">UpdatedBy</dt>
            <dd class="col-sm-10">@accommmodation.UpdatedBy</dd>
        </dl>
        <EditForm method="post" Model="accommmodation" OnValidSubmit="DeleteAccommmodation" FormName="delete" Enhance>
            <button type="submit" class="btn btn-danger" disabled="@(accommmodation is null)">Delete</button> |
            <a href="/accommmodations">Back to List</a>
        </EditForm>
    }
</div>

@code {
    private Accommmodation? accommmodation;

    [SupplyParameterFromQuery]
    private int Id { get; set; }

    protected override async Task OnInitializedAsync()
    {
        using var context = DbFactory.CreateDbContext();
        accommmodation = await context.Accommmodation.FirstOrDefaultAsync(m => m.Id == Id);

        if (accommmodation is null)
        {
            NavigationManager.NavigateTo("notfound");
        }
    }

    private async Task DeleteAccommmodation()
    {
        using var context = DbFactory.CreateDbContext();
        context.Accommmodation.Remove(accommmodation!);
        await context.SaveChangesAsync();
        NavigationManager.NavigateTo("/accommmodations");
    }
}
